# Dummy values so `helm template` (just lint) can render required fields.
postgresql:
  host: pg.example.invalid
authentik:
  authentik:
    postgresql:
      host: pg.example.invalid
      password: dummy
  oauthApp:
    clientId: dummy
    clientSecret: dummy
openwebui:
  admin:
    password: dummy
  postgres:
    password: dummy
initJob:
  image: {}
