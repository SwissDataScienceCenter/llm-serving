# Dummy values so `helm template` (just lint) can render required fields.
authentik:
  authentik:
    postgresql:
      host: pg.example.invalid
      password: dummy
  oauthApp:
    clientId: dummy
    clientSecret: dummy
openwebui:
  admin:
    password: dummy
  postgres:
    host: pg.example.invalid
    password: dummy
initJob:
  image: {}
